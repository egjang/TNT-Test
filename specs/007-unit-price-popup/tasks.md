# Tasks: 007-unit-price-popup

- [ ] Add API in `DashboardController` to aggregate by `empName`.
- [ ] Add money icon to `SalesPlanNew` header.
- [ ] Implement popup modal with ESC and X only.
- [ ] Fetch `/api/v1/dashboard/unit-amounts-by-emp` and render table.
- [ ] Manual verify on dev: login name present, numbers format.

