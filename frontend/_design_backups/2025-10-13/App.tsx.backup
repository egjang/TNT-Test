import React, { useState } from 'react'
import { ResizableColumns } from '../../src/components/ResizableColumns'
import { Menu } from '../../src/features/menu/Menu'
import { MainView } from '../../src/features/main/MainView'
import { KakaoMap } from '../../src/components/KakaoMap'
import { VisitPlanPanel } from '../../src/features/customer/VisitPlanPanel'
import { MissingTransactionsPanel } from '../../src/features/customer/MissingTransactionsPanel'
import { DemandTargetsPanel } from '../../src/features/demand/DemandTargetsPanel'

export default function AppBackup() {
  const [selectedKey, setSelectedKey] = useState<string>('calendar')
  return (
    <div className="app-root">
      <ResizableColumns minLeft={180} minRight={220}>
        <div className="pane left">
          <div className="pane-header">메뉴</div>
          <Menu selectedKey={selectedKey} onSelect={setSelectedKey} />
        </div>
        <div className="pane center">
          <div className="pane-header">업무</div>
          <MainView selectedKey={selectedKey} />
        </div>
        <div className="pane right">
          <div className="pane-header">우측 패널</div>
          {selectedKey === 'customer:my-activities' ? (
            <div style={{ display: 'flex', flexDirection: 'column', gap: 8, height: '100%' }}>
              <div style={{ flex: 1, minHeight: 0 }}>
                <VisitPlanPanel />
              </div>
            </div>
          ) : selectedKey === 'customer:list' ? (
            <div style={{ display: 'flex', flexDirection: 'column', gap: 8, height: '100%' }}>
              <div style={{ flex: 1, minHeight: 0 }}>
                <MissingTransactionsPanel />
              </div>
            </div>
          ) : selectedKey.startsWith('demand') ? (
            <div style={{ display: 'flex', flexDirection: 'column', gap: 8, height: '100%' }}>
              <div style={{ height: '50%', minHeight: 0 }}>
                <DemandTargetsPanel />
              </div>
            </div>
          ) : (
            <div className="placeholder">향후 기능을 위해 예약된 영역</div>
          )}
        </div>
      </ResizableColumns>
    </div>
  )
}

