---
description: Create a new database table DDL using standard terms
---
1. Read the standard terms and data types from `/home/egjang/vibe-lab/tnt_db_dic/dictionary/standards.json`.
2. Create a DDL for the requested table name.
   - Use the standard terms for column names and comments.
   - Use the standard data types defined in the dictionary.
   - Follow the PostgreSQL syntax.
3. Save the generated DDL to `/home/egjang/vibe-lab/tnt_db_dic/sql/postgres/public/[table_name].sql`.
